import 'package:flutter/material.dart';

class CateogriesScreen extends StatelessWidget {
  const CateogriesScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('cateogries',)),

    );
  }
}
