import 'package:flutter/material.dart';

class ProdectScreen extends StatelessWidget {
  const ProdectScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Prodect',)),

    );
  }
}
